#include<stdio.h>
#include<stdlib.h>


int main(){
	
	char tokens[] = {"*", "+", "/", "-"}
	int running = 1;

	while (running){
	
		printf(">>>")
		char equation[3];
		fgets(equation, sizeof(equation), stdin);
		for (int i=0; i<sizeof(tokens)/sizeof(char); i++){
			if (token[i] == equation[1]){
			
			}
		}
	}



}
